import pickle
import app
# AWS credentials
# AWS Lambda or whatever
# smiles needs to be imported or taken from the app.py -> pushed to AWS Sagemaker Endpoint
# LAMBDA /whatever to use the model to predict
# Generate Descriptors on AWS -> model



# predicted value gets sent back to the applicaation and then the result is displayed 

model = ampl.load_model('location')

